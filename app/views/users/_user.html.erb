<li>
  <%= gravatar_for user %>
  <%= link_to user.name, user %>
  <% if can_destroy_user? user %>
    | <%= link_to t("delete"), user, data: {turbo_method: :delete,
                                          turbo_confirm: t("you_sure?")} %>
  <% end %>
<li>
